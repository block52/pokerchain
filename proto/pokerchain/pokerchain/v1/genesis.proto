syntax = "proto3";
package pokerchain.pokerchain.v1;

import "amino/amino.proto";
import "gogoproto/gogo.proto";
import "pokerchain/pokerchain/v1/params.proto";

option go_package = "github.com/block52/pokerchain/x/pokerchain/types";

// GenesisState defines the pokerchain module's genesis state.
message GenesisState {
  // params defines all the parameters of the module.
  Params params = 1 [
    (gogoproto.nullable) = false,
    (amino.dont_omitempty) = true
  ];
}
